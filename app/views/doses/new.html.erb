<div class="container">
  <h1>Dose your <%= @cocktail.name %>!</h1>
  <%= simple_form_for [ @cocktail, @dose ] do |f| %>
    <%= f.input :description %>
    <%= f.input :ingredient, collection: Ingredient.all, include_hidden: false %>
    <%= f.submit %>
  <% end %>
</div>
